# Base URL of the GuardRail Rails API.
# The Rails CORS config already allows http://localhost:5173 (this dev server).
# Copy this file to `.env` and adjust if your API runs elsewhere.
VITE_API_URL=http://localhost:3000
